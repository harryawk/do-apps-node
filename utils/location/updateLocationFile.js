let { generalUpdateFile } = require('../../helpers/utils');

const updateLocationFile = async (request, requestBody) => {
  let result = requestBody;
  // new_assignment_file_2
  return result;
};

module.exports = {
  updateLocationFile,
};