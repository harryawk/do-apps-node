let { removeFileFromDB } = require('../../helpers/utils');

const removeFileInLocation = async (requestBody) => {
  let result = requestBody;
  // new_assignment_file_1
  return result;
};

module.exports = {
  removeFileInLocation,
};